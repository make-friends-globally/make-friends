import { defineConfig } from "astro/config";

// GitHub Pages: https://make-friends-globally.github.io/make-friends/
export default defineConfig({
  site: "https://make-friends-globally.github.io",
  base: "/make-friends",
  output: "static",
});
